plugins {
  id "com.gradle.build-scan" version "1.12.1"
}

buildScan {
    licenseAgreementUrl = "https://gradle.com/terms-of-service";
    licenseAgree = "yes"
}

apply plugin: 'java'
group "org.sample"
version "1.0"

dependencies {
    compile "org.sample:sub1:1.0"
}
